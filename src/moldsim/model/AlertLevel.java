package moldsim.model;

/**
 * Represents the severity level of a mold or sensor alert.
 */
public enum AlertLevel{
    /** Low severity alert level. */
    LOW,

    /** Medium severity alert level. */
    MEDIUM,

    /** High severity alert level. */
    HIGH,

    /** Critical severity alert level. */
    CRITICAL;
}